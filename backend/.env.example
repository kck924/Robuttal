# ===========================================
# Robuttal Backend Environment Variables
# ===========================================

# Environment
# Options: development | staging | production
ENVIRONMENT=development

# Database
# Local PostgreSQL
DATABASE_URL=postgresql+asyncpg://user:password@localhost:5432/robuttal

# Supabase (uncomment and update for production)
# DATABASE_URL=postgresql+asyncpg://postgres.[project-ref]:[password]@aws-0-[region].pooler.supabase.com:6543/postgres

# AI Provider API Keys
ANTHROPIC_API_KEY=sk-ant-...
OPENAI_API_KEY=sk-...
GOOGLE_API_KEY=...
MISTRAL_API_KEY=...

# CORS - comma-separated list of allowed origins
# In development, this is ignored and all origins are allowed
# In production, specify your frontend URLs
ALLOWED_ORIGINS=https://robuttal.vercel.app,https://www.robuttal.com

# App settings
DEBUG=false
TOPIC_SELECTION_MODE=hybrid

# ===========================================
# Deployment Notes
# ===========================================

# Railway Deployment:
# Set these environment variables in the Railway dashboard.
# DATABASE_URL can be set from a Railway PostgreSQL service
# or use an external database like Supabase.

# Render Deployment:
# Set environment variables in the Render dashboard.
# Use render.yaml for managed deployments.

# Supabase Database:
# 1. Create a new project at https://supabase.com
# 2. Go to Settings > Database
# 3. Copy the connection string (use "Connection pooling" for production)
# 4. Replace [YOUR-PASSWORD] with your database password
# 5. Change postgres:// to postgresql+asyncpg:// for SQLAlchemy

# Running Migrations:
# Migrations run automatically on deploy via the start command.
# To run manually: alembic upgrade head
